
>> Pertinência <<

- A pertinência é um tipo de relação entre um elemento e um conjunto.

- Ela indica a existência ou a ausência de um elemento dentro de um conjunto.
Para isso são utilizados dois símbolos de operadores relacionais:

∈, que significa "pertence";
∉, que significa "não-pertence"

- No caso das relações de pertinência, quando queremos afirmar que um elemento x pertence a um conjunto A qualquer, utilizamos o símbolo ∈, ou seja x ∈ A.

- Porém se o elemento x não pertence ao conjunto A. Utilizamos o símbolo ∉, ou seja, x ∉ A.

- Vejamos um exemplo: Consideremos o conjunto V das vogais do nosso alfabeto. V = {a, e, i, o, u}

- Dizemos que a vogal "a" pertence ao conjunto V formado pelas vogais do alfabeto, ou seja, a ∈ V.


>> Conjuntos vazio e universo <<

- Um conjunto vazio

- Representação é Ø ou { }

- É um conjunto que não contém elementos.

- Conjuto A mostrado a seguir: A = {x ∈ ℕ | x < 0}

- Não existem números naturais menores que 0

- Logo o conjunto A não tem elementos

- Nesse caso, o conjunto A é um conjunto vazio, ou seja  A = Ø


>> Subconjuntos e relação de inclusão <<

- Um subconjunto é um conjunto que integra outro.

- O diagrama da figura seguinte mostra o relacionamento entre um conjunto universo U com seus subconjuntos A e B.

- Nesse caso, costumamos dizer que "A está contido em U" assim como "B está contido eu U".

- Essa relação entre os conjuntos é o que chamamos de relação de inclusão.

- Existem dois principais símbolos de operadores relacionais que podemos utilizar nesse contexto: 

- ⊂, que significa “está
contido em”.

- ⊄, que significa “não está
contido em”.


>> Operações entre conjuntos <<

- As operações entre conjuntos são operações capazes de resultar em um novo conjunto a partir de outros já existentes.

- Estudaremos as seguintes operações: união, interseção, diferença e complementar.

União:

- Se A e B são conjuntos de A com B e denotada AՍB que representa o conjunto formado por todos os elementos de A e por todos os elementos de B.

- Graficamente, AՍB será composto pelos elementos que pertencem as regiões destacadas na figura 3, a seguir

União
Vejamos os exemplos a seguir:

- Seja A = {2, 4, 6, 8} e B = {1,3}; então, AUB = {1, 3, 2, 4, 6, 8}.

- {1, 2, 3} U {4, 5, 6} = {1, 2, 3, 4, 5, 6}

- {1, 2, 3} U {3, 4, 5} = {1, 2, 3, 4, 5} 

Interseção:

- A interseção de dois conjuntos A e B é descrita por A∩B, e é formada pelos elementos que pertencem tanto a A quanto a B, simultaneamente.

- Seja A = {2, 4, 6, 8} e B = {2,4}; então, A∩B = {2, 4}.

- {1, 2, 3} ∩ {4, 5, 6} = Ø

- {1, 2, 3} ∩ {3, 4, 5} = {3}


